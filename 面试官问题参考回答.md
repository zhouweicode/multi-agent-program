# Multi-Agent GraphRAG 项目面试问题参考回答

> 使用说明：以下回答严格区分“本项目已经实现的能力”和“合理的工程扩展方案”。面试时不要把尚未落地的医学多模态、Elasticsearch、RabbitMQ、Celery 等能力描述成项目现状。

## 一、知识图谱与实体消歧

### 1. 一个完整的知识图谱构建链路是什么样的？

我通常把完整链路分成九步：

1. **明确业务目标和本体**：先确定要回答什么问题，再定义 Scholar、Paper、Patent、Project、Enterprise、IndustrySegment 等实体，以及任职、作者、发明人、参与项目、企业合作等关系和约束。
2. **数据接入**：从业务数据库、开放数据、文档或 API 获取原始数据，并记录来源、批次和时间。
3. **清洗与标准化**：处理空值、乱码、重复记录、时间格式、单位和中英文名称等问题。
4. **信息抽取**：结构化表主要做字段映射；非结构化文档还需要实体识别、关系抽取和属性抽取。
5. **实体对齐与消歧**：利用姓名、机构、职称、上下文和向量召回找到候选实体，再生成全局稳定的 canonical ID，避免各数据库内部 ID 直接泄漏到业务层。
6. **关系构建与证据绑定**：把关系关联到来源记录、`evidence_id`、时间和置信度，保证结论可追溯。
7. **质量校验**：检查主键唯一性、必填字段、关系端点存在性、时间合法性、重复边和冲突事实；高风险冲突进入人工审核。
8. **存储与索引**：MySQL 保存权威结构化事实，Neo4j 保存图节点和关系，Milvus 保存实体检索向量及检索字段，并建立图索引、属性索引和向量索引。
9. **增量更新与评测**：用批次号或版本号完成幂等 upsert、删除传播和失败补偿，并持续评测消歧、检索、图查询和最终回答质量。

对应到本项目，MySQL 是学者和科研成果的结构化来源；`sync_milvus_entities.py` 将实体写入 Milvus；`sync_neo4j_research_graph.py` 将学者、论文及作者关系同步到 Neo4j；查询阶段统一使用 canonical ID 和 `EvidenceRecord`。不过，这个仓库重点是查询、编排和数据适配，不是一个已经覆盖文档抽取、人工审核、全量 CDC 的完整知识图谱生产平台。

### 2. 为什么同时使用 Dense、Sparse、ANN 和属性规则？

这几个概念解决的问题不同，ANN 也不是与 Dense、Sparse 并列的一种语义表示，而是大规模向量近邻搜索的方法。

- **Dense 向量**解决语义近似问题。它适合别名、简称、描述性表达和字面不同但语义相近的查询。例如“清华做知识图谱的张教授”和实体画像之间可能没有完整字面重合，但 Dense 表示仍可能把它召回。
- **Sparse 向量**保留精确词项信号。姓名、机构、职称、专有名词和缩写往往依赖字面匹配，Dense 有时会把语义相近但身份不同的人排得过高，Sparse 可以补足这部分能力。
- **ANN**用于在大规模 Dense 向量中快速找近邻，把近似的 Top-K 搜索从全量暴力比较变成可接受的延迟。本项目通过 Milvus `AUTOINDEX` 屏蔽具体索引实现，并用 COSINE 检索 Dense 向量。
- **属性规则**负责确定性校验和可解释重排。例如姓名精确匹配、机构命中加分、职称命中加分、Top1 绝对阈值以及 Top1/Top2 分差。这些规则防止一个较高的向量相似度覆盖权威身份事实。

本项目使用 BGE-M3 同时生成 1024 维 Dense Vector 和 Sparse lexical weights，Milvus 分别使用 COSINE 和 IP 检索，再用 RRF 融合。Hybrid 模式还会合并 MySQL 姓名精确召回，并依据机构、职称、阈值和分差决定自动确认还是要求用户选择。

### 3. 错误合并和错误拆分，哪个风险更大？

一般情况下，**错误合并的风险更大**，尤其是在医学、金融、科研评价等高风险场景。

错误合并是把两个人或两个概念错误地认成同一实体，会造成事实污染：A 的论文、任职或专利被归到 B 名下，而且污染后的关系会继续扩散到统计、图路径和模型回答中。错误拆分主要损害召回和完整性，通常还可以通过后续合并修复；错误合并则需要识别并拆除已经传播的错误关系，修复成本更高。

因此本项目采用偏保守策略：唯一的 MySQL 精确候选可以自动确认；多候选只有在 Top1 达到绝对阈值且与 Top2 分差足够时才自动确认，否则中断工作流，请用户选择。实际策略仍要根据业务成本调整，不能机械地认为所有场景都相同。

### 4. 错误拆分具体会导致什么问题？

错误拆分会把同一个真实对象分配到多个 canonical ID，主要导致：

- 同一个专家的论文、项目、专利和任职经历被分散，个人画像不完整；
- 共同论文数、合作年份跨度、中心性等指标被低估；
- 图中本来连通的路径断裂，最短路径、邻居和社区发现结果失真；
- 检索时出现多个高度相似候选，增加人工消歧成本；
- 更新和删除只命中其中一个实体，产生残留数据；
- 下游推荐、专家评价和关系推理出现假阴性。

治理上应保留 alias、来源 ID 和版本化映射，定期发现高度相似且关系互补的实体，经过规则或人工确认后执行可审计的合并，而不是直接覆盖历史 ID。

### 5. Top-10 召回率 95% 和自动匹配准确率 98% 分别是什么意思？怎么统计？

这两个指标衡量不同阶段：

**Recall@10** 表示有正确实体标注的 Query 中，正确实体出现在前 10 个候选里的比例：

```text
Recall@10 = 正确实体位于 Top-10 的样本数 / 有正确实体的总样本数
```

例如 1000 条有答案的标注 Query 中，有 950 条的正确实体进入前 10，Recall@10 就是 95%。它衡量的是“召回阶段有没有把正确答案捞上来”，不要求正确实体排第一。

**自动匹配准确率**表示系统实际执行了自动确认的样本中，确认正确的比例：

```text
Auto-match Accuracy = 自动确认正确数 / 系统执行自动确认的样本数
```

例如系统只对 800 条高置信样本自动确认，其中 784 条正确，准确率是 98%。这个指标必须和**自动匹配覆盖率**一起看；如果系统只自动确认最容易的少量样本，准确率会很高但业务价值有限：

```text
Auto-match Coverage = 自动确认样本数 / 所有可匹配样本数
```

统计时应先构建独立人工标注集，覆盖同名、别名、中英文名、机构、职称、拼写错误、描述性查询和不存在实体；按固定版本运行检索，避免在调阈值用的数据上报告最终结果；同时报告 Recall@10、Top1 Accuracy、自动确认准确率、覆盖率和拒识准确率。

需要诚实说明：**本仓库目前不能证明 95% 和 98% 是正式实测结果。** 项目确实有 `evaluate_entity_resolution.py` 计算这些指标，但现有样例集只有 5 条，更像评测管线 smoke test。面试时可以讲指标定义和统计方法，但不能把 95%/98% 当作已经由当前仓库验证的生产指标。若要正式宣称，应准备独立标注集、样本规模、数据切分、运行版本和逐例结果。

### 6. GraphRAG 与普通向量 RAG 有什么区别？

普通向量 RAG 的核心是“Query 向量化—召回相似文本块—让模型根据文本块回答”，擅长局部语义检索，但对多跳关系、全局结构和确定性约束支持较弱。

GraphRAG 把实体和关系显式建模为图，可以执行邻居查询、最短路径、K 跳扩展、关系强度和跨实体聚合。例如“专家 A 如何通过共同项目间接关联企业 B”需要沿 Scholar—Project—Enterprise 路径推理，仅靠相似文本很难稳定回答。

本项目实际上是混合方案：

- Milvus 用于实体候选召回和消歧；
- MySQL 提供论文、项目、专利、任职和教育等权威结构化事实；
- Neo4j 提供图邻居、多跳路径和企业、产业链关系；
- Multi-Agent 根据问题领域调用不同工具；
- Merge、Validator、Verification 和 Answer 只使用可追溯证据生成结论。

所以区别不只是“存储换成图数据库”，而是检索单元、推理方式、证据组织和校验机制都发生了变化。

## 二、Agent 系统设计

### 7. Agent 按什么思路拆分？是不是越细越好？

本项目按**业务边界、工具集合、数据源和验收事实类型**拆成五个领域 Agent：

1. TalentOrganizationAgent：专家画像、任职、教育和任职重叠；
2. ResearchAchievementAgent：论文、项目、专利和科研合作；
3. EnterpriseRelationAgent：企业角色、企业项目和企业专利；
4. IndustryChainAgent：产业链结构、节点企业和产业事件；
5. GraphReasoningAgent：邻居、多跳路径、K 跳子图和路径强度。

此外还有 VerificationAgent，专门验证“长期稳定合作”等无法靠单一确定性规则判断的复杂语义。

Agent 不是越细越好。拆分过粗会导致工具空间太大、Prompt 相互干扰、权限边界不清；拆分过细则会增加路由错误、网络往返、状态合并、上下文重复和分布式事务成本。我的判断标准是：一个 Agent 是否具有内聚的业务目标、相对稳定的工具白名单、清晰的数据权限，以及可以独立验收的输出协议。如果两个能力总是同时出现、共享相同数据和验收规则，就没有必要强行拆开。

### 8. Supervisor 如何判断直接路由、任务拆解还是重新 Plan？

这里要区分 Router、Supervisor 和 Validator 的职责：

1. **直接路由**：Router 先输出 `complexity`、`primary_domain` 和实体 mention。问题只涉及单一领域时，例如“张伟发表过哪些论文”，实体解析完成后直接进入 AchievementAgent，绕过 Supervisor，降低延迟和模型调用成本。
2. **任务拆解**：问题被判断为 `complex` 时进入 Supervisor。Supervisor 根据问题、已解析 canonical ID 和领域信号生成结构化 `PlannedTask`，每个任务包含目标、Agent、`required_fact_types` 和 `required_entity_ids`，然后由 LangGraph 动态 fan-out 并行执行。
3. **重新 Plan**：Agent 结果经 Merge 后由 Rule Validator 按任务契约验收。如果缺少领域、事实类型、目标实体或数据级约束不满足，Validator 返回 `needs_replan=true`、`missing_domains` 和错误列表；VerificationAgent 发现证据不足时也可以返回 `missing_evidence`。Supervisor 读取这些反馈和 `task_history`，只补调缺失领域，不全量重跑。

重规划有两个保护：只在确实缺少证据时发生；`replan_count < max_replans` 才允许回环，默认上限是 2，避免无限规划。若事实已齐全但结论本身不成立，例如两个人确实没有共同论文，则应该如实回答，而不是反复查询。

### 9. Agent 或图查询工具之间的权限隔离怎么实现？

本项目首先做的是**代码级最小工具权限**。每个 Agent 初始化时只绑定本领域工具，并在本地构造 `{tool_name: tool}` 白名单。模型即使生成其他工具名，执行层也找不到对应工具并返回“工具未授权”。例如 AchievementAgent 看不到 Neo4j 任意 Cypher 执行入口，只能调用预先定义的论文、项目和专利工具。

Repository 层继续收紧权限：MySQL 使用参数化 SQL 和只读事务；Neo4j 只暴露固定方法，不向模型开放任意 Cypher；Service 层统一做 canonical ID 转换和返回结构归一。任务契约和 Validator 再校验 Agent 是否返回了应该返回的事实，避免“调用成功但答非所问”。

不过当前安全仍属于教学级：API 鉴权、租户隔离、数据库账号分权、行级权限、审计告警和凭据轮换尚未完整实现。生产环境还应做到每类 Repository 使用独立只读账号、网络隔离、查询超时、结果条数限制、敏感字段脱敏和全链路审计。

## 三、多模态医学 RAG

### 10. 为什么优先解析原生文本，失败后再调用视觉模型？

先澄清：本项目是科技知识图谱 Multi-Agent GraphRAG，**没有实现医学 PDF 多模态解析**。如果扩展到医学文档，我会采用“原生文本优先、版面/OCR/视觉模型兜底”的分层方案。

原因是原生文本解析通常更快、更便宜、更稳定，能保留可搜索字符、页码、标题层级和部分阅读顺序，也更容易做精确引用。视觉模型适合扫描件、复杂表格、图中标注、异常排版和公式，但成本和延迟更高，还可能产生视觉幻觉。合理链路不是简单地“抽不到任何字才调用视觉模型”，而是先做页面质量判定：字符数、乱码率、文本覆盖率、阅读顺序、表格完整度和图片占比不达标时，只对问题页面执行 OCR、版面分析或视觉模型解析。

### 11. 文本、图片和图表之间的关联怎么实现？

我会使用统一的文档层级和稳定 ID：

```text
document_id
  └─ page_id
      ├─ block_id: text / title / caption / table / figure
      └─ asset_id: image / chart / table snapshot
```

解析时保留每个 block 的页码、坐标框、阅读顺序、章节路径和来源文件版本。图片与图表通过以下信号关联文本：

- 版面邻近关系：caption 与图片或表格的空间距离；
- 编号关系：图 2、Figure 2、表 3 等交叉引用；
- 章节关系：block 所属标题层级；
- 语义关系：caption、正文引用和视觉摘要的相似度；
- 显式图边：`TEXT_REFERS_TO_FIGURE`、`FIGURE_HAS_CAPTION`、`TABLE_BELONGS_TO_SECTION`。

检索时先召回文本或实体，再沿关联边取回图片、caption 和表格结构；提供给模型的证据必须包含 `document_id + version + page + bbox`，前端可以准确跳转到原文位置。

### 12. 是否考虑医学场景下大模型回答的风险？

医学场景必须按高风险系统设计。我的原则是让模型做“有证据的知识辅助”，不让它冒充医生做最终诊断。

- 只允许基于权威、版本明确的医学资料回答，并强制逐条引用；
- 区分患者教育、文献检索、临床辅助和诊断建议，不同风险级别采用不同策略；
- 对剂量、禁忌症、过敏、妊娠、儿童等高风险内容使用结构化规则或权威数据库二次校验；
- 证据不足、冲突或超出知识时明确拒答或升级人工医生；
- 输出不确定性、适用人群、资料日期和“不能替代医生”的边界；
- 做隐私保护、访问控制、审计、脱敏和数据最小化；
- 用医学专家标注集评测事实一致性、引用正确率、漏诊/误导风险和拒答能力，而不仅是语言流畅度。

本项目的可借鉴点是 `EvidenceRecord → Rule Validator → VerificationAgent → Answer` 的证据闭环，但这并不等价于已经满足医疗合规要求。

## 四、混合检索与数据一致性

### 13. Milvus、Elasticsearch 和 MySQL 是三路混合检索分支吗？

**在本项目中不是。** 当前实体消歧的 Hybrid 模式只有两类召回来源：

- MySQL：按姓名做权威精确召回；
- Milvus：Dense + Sparse 两个向量检索请求，并由 RRF 融合。

Neo4j 用于关系和路径查询，不属于同一个实体候选召回分支；项目没有接入 Elasticsearch。

如果未来增加 ES，可以设计为三类互补信号：MySQL 负责强约束和权威属性过滤，ES/BM25 负责全文关键词、短语和字段检索，Milvus 负责语义相似度；各分支先产生带来源和原始排名的候选，再通过 RRF 或经过校准的加权融合统一排序。不能直接相加未经校准的 BM25、COSINE 和数据库规则分数。

### 14. Milvus 中具体存储什么？

本项目的 `scholar_entities` Collection 每条记录代表一个学者实体，包含：

- `canonical_id`：主键；
- `name`：姓名；
- `organization`：机构；
- `title`：职称；
- `search_text`：由姓名、机构和职称拼接的检索文本；
- `dense_vector`：BGE-M3 生成的 1024 维 Dense Vector；
- `sparse_vector`：BGE-M3 生成的 lexical weights。

Milvus 在这里主要解决实体召回，不是科研成果全文知识库。权威论文、项目、专利和任职事实仍由 MySQL 或 Neo4j 查询。将 `canonical_id` 一起存入 Milvus，是为了召回后能回到统一实体以及其他后端，而不是把向量命中的记录直接当成最终事实。

### 15. 删除知识内容时，如何保证多种存储介质的一致性？

本项目目前没有实现覆盖 MySQL、文件、MongoDB、MinIO、ES 和 Milvus 的完整分布式删除流程，因此我会把下面内容描述为生产化方案，而不是现状。

核心思路是建立**唯一事实源、全局内容 ID、版本号和可重放事件**：

1. 在元数据主库中将内容标记为 `DELETING` 或写入 tombstone，而不是一开始物理删除；
2. 在同一数据库事务中通过 Outbox 写入 `KnowledgeDeleted(content_id, version)` 事件；
3. 异步消费者分别删除或失效 ES 文档、Milvus 向量、MongoDB 元数据、MinIO 对象和图关系；
4. 每个消费者用 `content_id + version` 实现幂等，重复消费不产生副作用；
5. 记录每个存储的处理状态，全部成功后再把主记录改成 `DELETED`；
6. 失败进入重试和死信队列，不允许静默丢失；
7. 定期做 reconciliation，对比主库清单、索引和对象存储，修复孤儿向量、孤儿文件和漏删索引；
8. 查询层始终校验主库状态或版本，确保删除传播期间旧索引也不会被展示。

这里追求的是最终一致性和业务不可见，而不是跨多种数据库强行做成本很高的分布式强事务。合规删除还要定义备份保留期、审计记录和不可恢复时间窗口。

### 16. 文档更新、旧向量和旧数据结构怎么处理？

本项目已经使用 `canonical_id` 做 Milvus upsert，并提供 MySQL→Milvus、MySQL→Neo4j 同步脚本，但还没有完整的文档版本和旧向量迁移系统。

生产环境中我会为每个文档保存 `document_id`、`content_hash`、`document_version`、`chunk_id`、`chunking_version`、`embedding_model`、`embedding_dimension` 和 `schema_version`：

- 内容不变时通过 hash 跳过重建；
- 内容变化时生成新版本 chunk 和向量，完成后原子切换 active version，再异步清理旧版本；
- 删除或拆分掉的 chunk 写 tombstone，避免旧向量残留；
- Embedding 模型或维度变化时创建新 Collection 或新字段，双写、回填、对比评测后切读，不能把不同模型的向量混在同一索引直接比较；
- Schema 演进采用向后兼容读取、默认值和显式迁移脚本；
- 为重建任务保存 checkpoint，使大批量更新可以断点续跑；
- 切换后执行数量、hash、抽样查询和召回指标校验。

## 五、异步任务与流式回答

### 17. RabbitMQ、Celery 和 APScheduler 各有什么特点？

它们不在同一抽象层：RabbitMQ 是消息代理，Celery 是分布式任务框架，APScheduler 是调度器。

| 组件 | 核心特点 | 适合任务 |
|---|---|---|
| RabbitMQ | 提供队列、Exchange、路由键、ACK、持久化、重试/死信等消息能力；本身不负责定义 Python 任务执行模型 | 服务解耦、可靠事件传递、复杂路由、削峰和任务投递 |
| Celery | 定义、分发和执行分布式任务，支持 worker、重试、超时、并发、结果后端和工作流；通常需要 RabbitMQ 或 Redis 作为 broker | PDF 解析、Embedding、索引构建、批量图谱同步、耗时模型任务 |
| APScheduler | 在应用进程中按日期、间隔或 Cron 触发函数，部署简单，但默认不等同于可靠的分布式任务队列 | 单实例定时清理、周期性统计、小规模同步触发器 |

常见组合是 APScheduler 或 Celery Beat 负责“何时触发”，Celery worker 负责“在哪里执行”，RabbitMQ 负责“如何可靠传递任务”。如果是多实例部署，要避免多个 APScheduler 实例重复调度，或者通过分布式锁、单独调度服务解决。

当前项目只使用进程内 `ThreadPoolExecutor + SQLite Run Registry + timeout/cancel signal`。它适合教学和单进程运行；生产化可以用 Celery/RQ/Arq 等替换后台执行层。

### 18. 流式回答时用户中途断开连接怎么办？

首先区分两种产品语义：

- **可恢复的后台任务**：断开 SSE/WebSocket 只代表客户端不再订阅，不应自动取消计算。服务端继续保存事件和结果，用户用 `run_id + cursor` 重连并补拉遗漏事件；只有用户显式点击取消、超时或策略要求时才停止任务。
- **一次性的直连 Token 流**：后端检测 `request.is_disconnected()` 后应取消上游模型请求、关闭 HTTP 连接和生成器，停止后续检索或工具调用，释放 GPU、线程、数据库游标和临时资源。

本项目属于第一种：Query 提交后在后台线程运行，SSE `/queries/{run_id}/stream` 只推送事件，客户端断开不会自动取消任务；用户可以重连，也可以调用 `/queries/{run_id}/cancel` 显式取消。RunManager 设置超时，并在 Node 边界检查协作式取消信号。

当前实现还可以改进：SSE 生成器可以显式捕获取消并记录订阅断开；长时间阻塞中的外部 LLM/数据库调用不能只依赖 Node 边界，应把取消信号传到支持 abort 的上游客户端，并为每个调用设置严格 timeout。

## 六、现场编程题

### 19. 现在方便共享屏幕，打开 IDE 做一道题吗？

可以。我的回答会简短直接：

> 可以。我先确认一下输入输出格式、标签是否固定、是否需要处理否定和冲突意图，然后我会先实现一个可运行的基线版本，再补测试用例并说明复杂度和可扩展点。

共享屏幕后建议一边写一边讲：先定义标签体系和样例，再实现文本归一化、规则匹配、否定处理、去重排序，最后运行测试。不要一开始就沉默写完整段代码。

### 20. 多标签意图识别现场实现

#### 题目设计

输入：一条用户 Query。

输出：零个或多个意图标签及分数。例如：

```text
输入：帮我查一下张伟和李明共同发表的论文，并分析他们有没有共同任职经历
输出：[
  {"label": "QUERY_COMMON_PAPERS", "score": 1.0},
  {"label": "QUERY_EMPLOYMENT_OVERLAP", "score": 1.0}
]
```

标签体系：

- `QUERY_PROFILE`：查询专家画像；
- `QUERY_PAPERS`：查询论文；
- `QUERY_COMMON_PAPERS`：查询共同论文；
- `QUERY_PROJECTS`：查询科研项目；
- `QUERY_PATENTS`：查询专利；
- `QUERY_EMPLOYMENT`：查询任职经历；
- `QUERY_EDUCATION`：查询教育经历；
- `QUERY_EMPLOYMENT_OVERLAP`：查询共同任职或时间重叠；
- `QUERY_ENTERPRISE`：查询企业关系；
- `QUERY_INDUSTRY_CHAIN`：查询产业链；
- `QUERY_GRAPH_PATH`：查询多跳路径或间接关系；
- `COMPARE`：比较多个对象；
- `VERIFY_COOPERATION`：验证长期或稳定合作关系。

#### Python 实现

```python
from __future__ import annotations

import re
from dataclasses import dataclass


@dataclass(frozen=True)
class IntentRule:
    label: str
    keywords: tuple[str, ...]
    required_any: tuple[str, ...] = ()
    weight: float = 1.0


RULES = (
    # 更具体的组合意图放在前面，便于后续抑制宽泛标签。
    IntentRule(
        "QUERY_COMMON_PAPERS",
        ("共同论文", "共同发表", "合著论文", "合作论文"),
    ),
    IntentRule(
        "QUERY_EMPLOYMENT_OVERLAP",
        ("共同任职", "任职重叠", "共事", "同事"),
    ),
    IntentRule(
        "VERIFY_COOPERATION",
        ("长期合作", "稳定合作", "持续合作", "合作伙伴"),
    ),
    IntentRule(
        "QUERY_GRAPH_PATH",
        ("多跳", "路径", "间接关系", "怎么关联", "关系强度"),
    ),
    IntentRule(
        "QUERY_INDUSTRY_CHAIN",
        ("产业链", "产业节点", "产业事件", "产业全景"),
    ),
    IntentRule(
        "QUERY_ENTERPRISE",
        ("企业关系", "企业任职", "公司任职", "企业项目", "企业专利", "技术合作"),
    ),
    IntentRule("QUERY_EDUCATION", ("教育经历", "教育背景", "毕业院校", "学历", "学位")),
    IntentRule("QUERY_EMPLOYMENT", ("任职经历", "工作经历", "工作单位", "履历", "任职")),
    IntentRule("QUERY_PROJECTS", ("科研项目", "研究项目", "参与项目", "主持项目", "项目")),
    IntentRule("QUERY_PATENTS", ("共同专利", "申请专利", "发明专利", "专利")),
    IntentRule("QUERY_PAPERS", ("发表论文", "论文", "文章", "著作")),
    IntentRule("QUERY_PROFILE", ("专家画像", "个人资料", "基本信息", "介绍")),
    IntentRule("COMPARE", ("比较", "对比", "哪个更", "差异", "分别分析")),
)

NEGATIONS = ("不要", "不用", "无需", "不需要", "别查", "不查询")


def normalize(text: str) -> str:
    """统一大小写和空白，保留中文以及关键词需要的标点边界。"""
    return re.sub(r"\s+", "", text.strip().lower())


def is_negated(text: str, start: int) -> bool:
    """检查当前分句中、关键词之前是否出现否定词。"""
    prefix = text[:start]
    # 标点或转折词会结束前一个否定作用域。
    boundaries = ("，", ",", "。", ".", "；", ";", "！", "!", "？", "?",
                  "但是", "但", "而是")
    boundary = max((prefix.rfind(item) + len(item) for item in boundaries), default=0)
    clause_prefix = prefix[boundary:]
    return any(word in clause_prefix for word in NEGATIONS)


def match_rule(text: str, rule: IntentRule) -> tuple[bool, list[str]]:
    hits: list[str] = []
    for keyword in rule.keywords:
        for match in re.finditer(re.escape(keyword), text):
            if not is_negated(text, match.start()):
                hits.append(keyword)
                break

    if not hits:
        return False, []
    if rule.required_any and not any(word in text for word in rule.required_any):
        return False, []
    return True, hits


def recognize_intents(query: str) -> list[dict]:
    text = normalize(query)
    results: list[dict] = []

    for rule in RULES:
        matched, evidence = match_rule(text, rule)
        if matched:
            # 多个独立关键词命中时小幅加分，上限为 1。
            score = min(1.0, rule.weight * (0.8 + 0.1 * len(set(evidence))))
            results.append({
                "label": rule.label,
                "score": round(score, 2),
                "evidence": sorted(set(evidence)),
            })

    labels = {item["label"] for item in results}

    # 具体意图已命中时，抑制被同一短语顺带命中的宽泛意图。
    if "QUERY_COMMON_PAPERS" in labels:
        results = [item for item in results if item["label"] != "QUERY_PAPERS"]
    if "QUERY_EMPLOYMENT_OVERLAP" in labels:
        results = [item for item in results if item["label"] != "QUERY_EMPLOYMENT"]

    return sorted(results, key=lambda item: (-item["score"], item["label"]))


if __name__ == "__main__":
    cases = [
        "帮我查张伟和李明共同发表的论文，并分析他们有没有共同任职经历",
        "比较两人的教育背景、科研项目和专利",
        "查询张伟到某企业的多跳路径，同时看看这家企业处于哪个产业链节点",
        "只看论文，不要查询专利和任职经历",
        "他们是不是长期稳定的合作伙伴？请结合共同论文和项目验证",
    ]
    for case in cases:
        print(case)
        print(recognize_intents(case))
```

#### 预期结果说明

- 第一条同时命中共同论文和共同任职，是典型多标签输出；
- 第二条命中比较、教育、项目和专利；
- 第三条命中图路径和产业链；
- 第四条只保留论文，否定窗口会抑制专利和任职；
- 第五条命中合作验证、共同论文和项目。

#### 复杂度与进一步改进

设规则数为 `R`，每条规则平均关键词数为 `K`，Query 长度为 `N`，这个基线实现可近似看成 `O(R × K × N)`；当前标签规模很小时完全可接受。规则量增大后可使用 Trie/Aho-Corasick 做一次扫描，但这不是现场题的必要复杂度。

这个实现的优点是可解释、无训练依赖、容易测试；局限是对隐式表达、长距离否定和上下文依赖不够强。生产系统可以在保留规则 Guardrail 的基础上增加训练好的多标签分类模型或结构化 LLM，并用人工标注集评测每个标签的 Precision、Recall、F1、Micro-F1、Macro-F1 和 Exact Match Ratio。

## 面试时建议主动说明的项目边界

1. 当前本地运行是“真实模型 + Milvus 实体检索 + 其他部分仍可使用 Mock”的混合状态，不要说全部真实后端已经上线。
2. 95% Recall@10 和 98% 自动确认准确率没有被当前小型评测集正式证明，不能当作已验证生产数据。
3. 项目没有接入 Elasticsearch、MongoDB、MinIO、RabbitMQ、Celery、APScheduler，也不是医学多模态项目；相关回答属于生产化扩展设计。
4. 项目已有 Agent 工具白名单和 Repository 只读约束，但 API 鉴权、租户隔离和数据库行级权限仍需补充。
5. 项目当前的 SSE 是运行事件流，不是大模型逐 Token 直出流；断线后后台 Run 默认继续执行并支持重连查询。
